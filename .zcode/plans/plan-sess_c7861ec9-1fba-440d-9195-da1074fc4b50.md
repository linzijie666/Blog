## 修复方案：5 项技术错误 + 文字清理（图片保留、懒加载不做）

### 1. DDR4 容量示例（DdrArticle.jsx:46）【高】
按配图红框 256Mb×16 列改写：`2 个 Bank Group（BG 地址仅 BG0 一根）× 4 BANK（BA0~BA1）× 行地址 A0~A14 × 列地址 A0~A9 × 16bit 位宽`，并给出 2^(1+2+15+10)×16=2³²=4Gb；补一句同容量 x4 配置为 4 BG（BG0~1）× 行 A0~A15 × 4bit，避免再混列。

### 2. MAX809 阈值分档（ResetWatchdogArticle.jsx:22、25）【高】
- 正文改为：L=4.63V、M=4.38V、T=3.08V、S=2.93V、R=2.63V，"3.3V 系统通常选 2.93V 的 S 档"。
- 图注"L 档 2.93V 红框"改为"M 4.38V、S 2.93V、Z 2.32V 红框"（与配图红框实际位置一致）。

### 3. 开关电源三处（SwitchingRegulatorArticle.jsx）【中】
- 第 15 行："非同步内置二极管续流" → "非同步外置二极管续流"（与 B550A 配图一致）。
- 第 38 行："常用在大电流或大压差场景" → "常见于中小电流、成本敏感的场合"，消除与同句后半段及面试答案的自相矛盾。
- 第 102 行：RC 延时改为 `t=τ×ln(5/(5−1.2))≈0.27τ≈1.3ms`（原"0.4τ≈1.9ms"错误：0.4τ 时电容已到 1.65V）。

### 4. 两级电源树损耗归因（LinearRegulatorArticle.jsx:102）【中】
"（DC-DC 承担大头）"改为正确归因并给出数字：LDO 承担大头（1.7V×0.5A≈0.85W），90% 效率的 DC-DC 仅约 0.28W，两颗发热都远低于 12V 直降的 4.35W。

### 5. 晶振频偏表述（McuArticle.jsx:34、44）【中】
删除无依据的"频偏在 ±10ppm 容差内"：改为"计算值 16pF 恰为 E24 标称值可直接采用；实取 18pF 时实际 CL≈13pF，约 1pF 负载偏差带来负向频偏，幅度需查 pullability 曲线或实测确认"（与原理图 18pF 保持一致）。

### 6. 文字清理（4 处粘连，不动图片）
- InductorArticle.jsx:18、64：`I_{rms}² ×DCR` → `I_{rms}² × DCR`
- OptocouplerArticle.jsx:44：`≈1KΩ` → `≈ 1kΩ`
- TriodeArticle.jsx:27：`U_O↑ →U_N↑ …` → 加空格
- SwitchingRegulatorArticle.jsx:82：`→V_{OUT}` → ` → V_{OUT}`

### 验证
- `npm test`：54 个测试应全部通过（已核对测试只做宽泛断言，不会受影响）。
- `npm run build`：生产构建通过。
- grep 复查无残留粘连写法。

不改 README（复核已确认其与实际一致）、不删 3 组未引用配图、不做懒加载（留待后续）。
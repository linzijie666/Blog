# 知识专栏分章改造：第一章 无源器件 / 第二章 基础半导体器件

## 目标结构

- **第一章 · 无源器件**（现有 4 篇）：电阻 resistor、电容 capacitor、电感 inductor、磁珠 ferrite-bead + 老版电容电感延伸阅读
- **第二章 · 基础半导体器件**（1 篇迁移 + 3 篇新增）：二极管 diode（迁入统一管理）、三极管 triode、光耦 optocoupler、MOS 管 mosfet
- 全部 8 篇进入统一 registry，形成线性 上一篇/下一篇 链（ferrite-bead → diode 跨章衔接）
- 第二章素材：`C:\Users\ASUS\OneDrive\图文课件-第二章-基础半导体器件`（60 页水印 JPG + 17.5MB PDF，页码与图片序号一一对应，12 个小节，覆盖二极管/三极管/光耦+OC-OD门/MOS+SOA）

## 第一步：素材加工

1. 新建 `scripts/build_semiconductor_article_assets.py`（仿照现有 passive 脚本，60 页校验），输出到 `public/images/knowledge/semiconductor-devices/`（webp 1400px + `-hd.jpg` 2200px）。配图候选页（实施时逐页查看微调裁剪框）：
   - 二极管：p5 肖特基防反接电路+datasheet、p9 稳压管参考源限流计算、p11-12 TVS（补 2-3 张给现有二极管文章）
   - 三极管：p14 NPN 开关+电平转换双图、p17 TL431 基准+线性稳压框图、p20 共射静态工作点、p24/p28 微变等效电路、p27-29 温度补偿
   - 光耦：p31 CTR-IF 曲线+参数表、p32-34 电路设计、p35 OC/OD 门与线与对比
   - MOS：p37-39 功能电路、p40 PMOS 缓启动、p46 MOS/三极管温度特性对比、p50 缓启动仿真波形、p55 损耗计算、p58-59 SOA
2. 拷贝 PDF → `public/downloads/semiconductor-devices-review.pdf`

## 第二步：articles.js 分章架构

- 每篇文章定义增加 `chapter` 字段 + 新增导出 `knowledgeChapters`（第一章 无源器件 / 第二章 基础半导体器件，含标题、简介、下载文件名、页数）
- `category` 按 chapter 生成："第一章 · 无源器件 / 硬件面试复习"、"第二章 · 基础半导体器件 / 硬件面试复习"
- 新增 registry 条目：`diode`（沿用现标题"二极管：从 PN 结到整流、限幅与稳压"、9 个 section、约 30–45 分钟）、`triode`（三极管：功能、放大电路三组态与工作点计算、温度补偿）、`optocoupler`（光耦：CTR 参数、电路设计、OC/OD 门与线与）、`mosfet`（MOS：功能电路、与三极管区别、并联、缓启动、参数选型与损耗、SOA）
- 每篇增加 `download` 信息（第一章 passive-components-review.pdf 44 页 / 第二章 semiconductor-devices-review.pdf 60 页），供 ArticleShell 和专栏页使用

## 第三步：二极管迁入统一管理

- DiodeArticle.jsx 拆成"正文组件"（sections 部分原样保留，公式下标渲染保留），页头/目录/标题由 ArticleShell 提供；ReviewArticle 的 `articleBodies` 增加 `diode` 映射
- route.js：diode 加入 registry 后移除 `resolveKnowledgeRoute` 的 diode 特判，`DIODE_ARTICLE_HASH` 改为从 registry 派生（保留导出名）
- App.jsx：移除 `knowledgeRoute === "diode"` 分支（自动走 ReviewArticle 通用路径）

## 第四步：三篇新文章组件（对齐第一章深度）

新建 `src/knowledge/articles/TriodeArticle.jsx`、`OptocouplerArticle.jsx`、`MosfetArticle.jsx`，沿用现有原语（ArticleFigure/ArticleFigureGroup/WorkedExample/FormulaText 下标语法/面试自测 details），每篇含：
- 三极管：原理与功能电路、静态工作点与三种组态（共射/共集/共基）对比、多级耦合、温度补偿；WorkedExample 用 NPN 开关饱和校核（Ib=(3.3-0.7)/1K=2.6mA 判饱和，取自课件 p14 数据）
- 光耦：功能与引脚、CTR 与关键参数、原边限流与电路设计、OC/OD 门与线与；WorkedExample 用光耦 CTR 校核
- MOS：功能电路（开关/防反/缓启动/开关电源/电机/恒流恒压）、与三极管区别（温度特性正/负反馈）、并联使用、缓启动设计、参数选型与损耗计算（导通/开关/驱动）、SOA；WorkedExample 用 MOS 损耗或缓启动 RC 估算

## 第五步：专栏页与文章壳分章

- KnowledgeSection.jsx 重构为两章分组：每章一组（章节标题 + 简介 + 4 张卡片 + 下载该章课件按钮），第一章组内保留延伸阅读入口；卡片图标补齐（diode=Zap、triode=Activity、optocoupler=Lightbulb、mosfet=ToggleLeft，避免 Icon undefined 崩溃）；卡片序号改为章内 01-04
- ArticleShell.jsx：下载链接与"44 页"文案改为读取 `article.download`（"下载 44/60 页原始复习资料…"）
- knowledge.css 增加章节分组标题样式（沿用现有设计语言）

## 第六步：测试更新

- knowledge-registry.test.mjs：slug 清单加 3 个（8 篇 + legacy）、重写 previous/next 链断言
- knowledge-route.test.mjs：ARTICLE_HASHES 补 diode/triode/optocoupler/mosfet，diode 特判断言改为 registry 路径
- knowledge-content.test.mjs：diode 相关断言（L292-306）改写为新架构等价断言；新增三篇文章的内容测试（仿四篇现有模式：主题覆盖 + WorkedExample 数量 + registry readingTime）；enhanced-assets 名单扩入半导体配图；ArticleShell 下载文案断言同步
- 新增配图必须先落盘（图片存在性校验自动生效）

## 第七步：README 更新

- 功能概览 Knowledge 专栏条目改为两章结构
- 当前展示内容：补三极管/光耦/MOS 三条，二极管条目归入第二章表述
- 项目结构：knowledge 目录说明更新
- "修改知识专栏"：文件清单、Hash 地址清单补 4 个（diode 原有 + 3 新）、新增文章流程补第二章素材目录与出图脚本
- 上线前检查：两章 8 篇 + 两份 PDF 下载检查

## 验证

- `node --test tests/*.test.mjs` 全绿
- 浏览器逐页验证：专栏页两章分组、8 篇文章渲染与前后翻页、两章 PDF 下载链接、移动端视口
- 全程图片/文案与课件页码对应（sourcePage 标注）